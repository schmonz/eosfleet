---
# clight is installed but intentionally not enabled or autostarted.
# See TODO.md before making it operational.
- name: Install clight
  kewlfft.aur.aur:
    name:
      - iio-sensor-proxy
      - clight
    use: yay
    state: present
  become: true
  become_user: "{{ target_user }}"
